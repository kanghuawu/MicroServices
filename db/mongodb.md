# MongoDB

```
db.collection.insert()

db.user.insert({
    _id: "1",
    firstName: "kh",
    lastName: "wu",
    credit: 100,
    orders: []
    })

db.collection.find()

db.collection.remove({})

db.collection.remove({"_id": "1"})

db.dropDatabase()
```